#include "shell.h"

int main(void)
{
	while(1)
	{
		check if user input something
		see if it makes sence
		if not 
			continue;
		use what they input as command
		execute command
		return whatever the command did
		if user exits end program
			exit(97);
	}
	return (0);
}
